# .NET
bin/
obj/
*.user
*.suo
.vs/
*.cache
publish/
appsettings.*.local.json
# Local secrets — real connection string / SMTP creds live here, never committed
appsettings.Development.json

# Uploaded media (written at runtime; never committed)
Solution/GAC.Web/wwwroot/uploads/

# Node (build scripts / tooling)
node_modules/

# OS
Thumbs.db
.DS_Store

# GAC static-site reference: keep pages/partials/assets/pdfs (porting source for
# later phases), exclude heavy dev artifacts (screenshots, research probes)
HTML/docs/
HTML/shots/
